{
  "name": "poolman-cards",
  "version": "0.1.0",
  "description": "Pool Manager Lovelace cards bundle (overview, problems, recommendations, action history, quick actions) for the Pool Manager Home Assistant integration.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^9.0.0",
    "happy-dom": "^20.0.0",
    "typescript": "^5.5.0",
    "vite": "^7.0.0",
    "vitest": "^4.0.0"
  }
}
